apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

nameSuffix: -staging

patches:
  - path: replica-patch.yaml
    target:
      kind: Deployment
      name: hello-service

patches:
  - path: configmap-patch.yaml
